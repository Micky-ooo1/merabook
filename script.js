// Function to open page for Physics
function openPage(subject) {
    if (subject === "Physics") {
        window.location.href = "/common_repo/class11physics.html";
    } else {
        alert("You selected: " + subject);
    }
}